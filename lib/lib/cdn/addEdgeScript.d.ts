export default class addEdgeScript {
    private client;
    constructor(accessKeyId: string, accessKeySecret: string);
    init({ domain, fcDomain }: {
        domain: any;
        fcDomain: any;
    }): Promise<void>;
}

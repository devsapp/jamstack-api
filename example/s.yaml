edition: 1.0.0
name: appName #  项目名称
access: default #  秘钥别名

services:
  start-function:
    component: ${path(..)}
    props:
      sourceCode: src
      app:
        environmentVariables:
          tablestore_endpoint: https://s-table-01.cn-hangzhou.ots.aliyuncs.com
          tablestore_instanceName: s-table-01
      route:
        - / # /index
        - /link

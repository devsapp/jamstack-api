edition: 1.0.0
name: appName #  项目名称
access: default #  秘钥别名

services:
  rest-api:
    component: ../lib
    props:
      region: cn-hangzhou
      app:
        name: rest-api-demo
      sourceCode: functions
      route:
        - /index
